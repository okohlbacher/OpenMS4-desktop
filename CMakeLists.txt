cmake_minimum_required(VERSION 3.24)
project(OpenMSDesktop VERSION 1.0.0 LANGUAGES CXX)
include(CTest)
option(OPENMS_DESKTOP_BUILD_GUI "Build the shared GUI implementation" ON)
option(OPENMS_DESKTOP_BUILD_VIEWERS "Build TOPPView, ImageCreator and INIFileEditor" ON)
option(OPENMS_DESKTOP_BUILD_WORKFLOWS "Build TOPPAS and ExecutePipeline" ON)
if(OPENMS_DESKTOP_BUILD_GUI)
  add_subdirectory(gui)
endif()
if(OPENMS_DESKTOP_BUILD_VIEWERS)
  add_subdirectory(viewers)
endif()
if(OPENMS_DESKTOP_BUILD_WORKFLOWS)
  add_subdirectory(workflows)
endif()
